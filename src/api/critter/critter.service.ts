type Critter = {
  critter_id: string;
  taxon_id: string;
  //blah blah blah
};

const getCritter = () => {
  //Place holder for prisma code
  return { id: 1, species: "caribou", location: "bc" };
};

export { getCritter };
